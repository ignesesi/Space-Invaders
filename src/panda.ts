/*import * as PIXI from "pixi.js";
import { Settings } from "./settings";

export class Panda extends PIXI.Sprite {
    public deltaX: number;

    constructor() {
        super(PIXI.Texture.from("panda.png"));
        //stage.addChild(panda);
        this.width = Settings.PANDA_WIDTH;
        this.height = Settings.PANDA_HEIGHT;
        this.x = (Settings.GAME_WIDTH - this.width) / 2;
        this.y = Settings.GAME_HEIGHT - 2 * this.height;
        this.interactive = true;
        this.deltaX = 0;
    }
}*/